# NPM Configuration for Render.com Deployment
# Clean npm configuration for cloud deployment

# Allow npm to work in restricted environments
unsafe-perm=true

# Skip optional dependencies to avoid errors
optional=false

# Ensure strict peer dependencies
strict-peer-dependencies=false

# Enable production optimizations
production=true
